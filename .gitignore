logs/

# Secrets — never commit
.env
.env.*

# gateway per-user keystore (secrets)
gateway_keys.json

# virtualenvs
venv/
venv-gen/

# large / regenerable build artifacts (use a model registry or HF, not git)
llama.cpp/
models/
adapters/
adapters14b/
adaptersG4/
data/
out/

# caches / editor / local
__pycache__/
*.pyc
.claude/
*.bak
serve_vllm_active.sh
*.7b-backup
venv-g4/
serve_vllm_active.sh.qwen-rollback
serve_vllm_active.sh.bnb-rollback

# wrangler (Cloudflare Pages) local cache
.wrangler/

# generated preview twin of the research page (regenerate from site/index.html)
site/research.artifact.html
llama.cpp/build-cuda/
models/shieldgemma-2b-gguf/
